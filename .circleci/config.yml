version: 2.1

jobs:
  serverTests:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: sudo npm install -g npm@latest
      - run: npm test

workflows:
  version: 2.1
  test:
    jobs:
      - serverTests